# Opt-in — embeddings reais no pgvector (F-GALILEO-1, ADR-031). NÃO é pré-requisito da app: o
# default é o `VegaKeywordRetriever` em processo, que não precisa de banco nem destas deps.
# Instale junto com o profile `rag` do compose quando quiser demonstrar embeddings de verdade.
# `langchain-openai` (embeddings) já vem no requirements.txt base.
langchain-postgres==0.0.17
langchain-ollama==1.1.0
psycopg[binary]==3.3.4
