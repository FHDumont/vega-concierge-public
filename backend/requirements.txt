# Versões fixadas (==) → CI usa EXATAMENTE as mesmas do ambiente local (F-036).
# Últimas estáveis em 2026-07-10, sob Python 3.13 (= Dockerfile).
langgraph==1.2.6
langchain-core==1.4.8
langchain-openai==1.3.3
langchain-anthropic==1.4.7
openai==2.45.0
anthropic==0.116.0
boto3==1.35.99
fastapi==0.138.0
uvicorn==0.49.0
# Observabilidade Splunk Agent Observability (ADR-032). Sem GALILEO_API_KEY o SDK nunca é chamado — fica instalado e
# inerte, e o import é guardado, então a app também sobe se o pacote não estiver presente.
galileo==2.6.0
# Agent Control / Protect (ADR-033). Sem GALILEO_API_KEY ou init falho → galileo_control no-op.
agent-control-sdk[galileo]==8.4.0
