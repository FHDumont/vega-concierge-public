# Pinned versions (==) → CI uses EXACTLY the same ones as the local environment (F-036).
# Latest stable as of 2026-07-10, under Python 3.13 (= Dockerfile).
langgraph==1.2.6
langchain-core==1.4.8
langchain-openai==1.3.3
langchain-anthropic==1.4.7
openai==2.45.0
anthropic==0.116.0
boto3==1.35.99
fastapi==0.138.0
# Environment-first config layer (F-BACKEND-1, app/settings.py).
pydantic-settings==2.14.2
uvicorn==0.49.0
# Splunk Agent Observability instrumentation (ADR-032). Without GALILEO_API_KEY the SDK is never called — it stays
# installed and inert, and the import is guarded, so the app still boots if the package isn't present.
galileo==2.6.0
# Agent Control / Protect (ADR-033). Without GALILEO_API_KEY or a failed init → galileo_control no-ops.
agent-control-sdk[galileo]==8.4.0
