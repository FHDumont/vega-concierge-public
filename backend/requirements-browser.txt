# OPTIONAL dependency for the simulator's BROWSER mode (F-039) — NOT in the base image.
# Install on demand (local or container) to avoid bloating the default backend:
#   pip install -r requirements-browser.txt && playwright install chromium
# Pinned version (==), validated on 2026-06-23 under Python 3.13. Matches the browser
# image tag (backend/Dockerfile.browser → mcr.microsoft.com/playwright/python:v1.60.0).
playwright==1.60.0
